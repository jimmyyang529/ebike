<%= form_for @item, :url => { :controller => 'admin/items', :action => 'update', :id => @item } do |f| %>
    <%= render :partial => 'form', :locals => { :f => f } %>
    <%= f.submit "Update" %>
<% end %>
